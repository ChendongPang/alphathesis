# Tests

Integration tests live under `tests/integration`.

Go unit tests stay next to their packages as `*_test.go` files. That is the
standard Go layout and lets tests exercise package internals without exporting
implementation details just for testing.
